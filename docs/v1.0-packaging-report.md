# CodeXX v1.0 打包检查报告

> 检查日期：2026-07-05
> 检查对象：`release/AIChatDesktop/` (71 MB)

---

## 已知问题

| # | 问题 | 严重度 | 处理建议 |
|:-:|------|:-----:|---------|
| 1 | Python sidecar 未打包到 release 目录 | 低 | Sidecar 模式需另安装 Python 环境，当前定位为可选能力，可接受 |
| 2 | Release zip 命名建议标准化为 `AIChatDesktop-1.0-windows.zip` | 低 | 重命名一致 |

---

## 本轮已修复

| # | 问题 | 状态 | 说明 |
|:-:|------|:---:|------|
| 1 | 缺少 `Qt6Concurrent.dll`（#25 新增依赖） | ✅ 2026-07-05 | 已从 `D:/QT/6.10.2/mingw_64/bin/Qt6Concurrent.dll` 补入 `release/AIChatDesktop/` |
| 2 | v1.0 zip 包仍是旧内容 | ✅ 2026-07-05 | 已用 `Compress-Archive` 重新生成 `release/AIChatDesktop-1.0-windows.zip`，并确认包含 `Qt6Concurrent.dll` |

---

## 已确认项目

| 项 | 状态 |
|---|:---:|
| AIChatDesktop.exe (2.7 MB) | ✅ |
| Qt6Core.dll / Qt6Gui.dll / Qt6Widgets.dll / Qt6Network.dll / Qt6Sql.dll / Qt6Svg.dll | ✅ |
| Qt6Concurrent.dll | ✅ |
| platforms/qwindows.dll | ✅ |
| imageformats/ (多个格式) | ✅ |
| tls/ (qcertonlybackend + qschannelbackend) | ✅ |
| styles/ (qwindowsvistastyle) | ✅ |
| translations/ (qt_*.qm) | ✅ |
| sqldrivers/ (qsqlite) | ✅ |
| MinGW 运行时 (libgcc/ libstdc++/ libwinpthread) | ✅ |
| opengl32sw.dll (软件 OpenGL) | ✅ |
| D3Dcompiler_47.dll | ✅ |

---

## 下次打包注意事项

```powershell
# 构建 Release
cmake --build build-release-qt --config Release -j4

# 打包
windeployqt build-release-qt/AIChatDesktop.exe --dir release/AIChatDesktop

# 检查 Qt6Concurrent 是否已包含
if (!(Test-Path release/AIChatDesktop/Qt6Concurrent.dll)) { throw "Missing Qt6Concurrent.dll" }

# 压缩
cd release
Remove-Item AIChatDesktop-1.0-windows.zip -ErrorAction SilentlyContinue
7z a AIChatDesktop-1.0-windows.zip ./AIChatDesktop/
```
