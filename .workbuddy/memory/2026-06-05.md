# 2026-06-05 工作日志

## T04: UI 测试补充 — ChatViewTest + MainWindowSmokeTest

### 背景
V15 ApplicationController 重构后，T03/T04 待实施。T04 目标为补充 UI 层测试覆盖。扫描发现两个核心类完全无测试：ChatView 和 MainWindow。

### 完成内容
1. **ChatViewTest** (`tests/ui/ChatViewTest.cpp`) — 16 个测试用例
   - 构造验证（scroll area/container/token bar/typing indicator）
   - addMessage（3 种角色）
   - addMessage with messageId 映射
   - widgetForMessageId / removeMessagesFrom
   - updateLastAssistantMessage（流式更新 + 自动创建）
   - clearMessages（清空 + 映射清理）
   - updateTokenUsage / showSearchBar / hideSearchBar / showTyping / hideTyping
   - addDebugCard / addAgentStepWidget

2. **MainWindowSmokeTest** (`tests/ui/MainWindowSmokeTest.cpp`) — 10 个测试用例
   - 构造不崩溃（含 ApplicationController::initialize()）
   - 核心控件验证（chatView/sessionList/messageInput/sendButton/modeToggle 等）
   - 模式切换按钮、发送按钮状态、主题切换按钮
   - 侧边栏操作按钮（6 个）、顶部工具栏按钮（3 个）
   - 状态栏、窗口属性、会话搜索输入框、析构不崩溃

3. **CMakeLists.txt** 更新：新增 ChatViewTest 和 MainWindowSmokeTest 目标
   - ChatViewTest: Qt6::Core + Qt6::Widgets + codexx_ui
   - MainWindowSmokeTest: Qt6::Core + Qt6::Widgets + Qt6::Network + Qt6::Sql + codexx_ui + codexx_app + (WIN32) Advapi32

### 发现
- `ChatView::messageCount()` = `m_contentLayout->count() - 1`，计数**所有**布局子条目（包括 debug card 和 agent step widget），并非仅 MessageWidget。测试已按实际行为验证。
- MainWindow 构造链路正常：setupUi → connectController → m_controller.initialize()，首次启动无配置文件时也可安全完成。

### 测试结果
- ChatViewTest: 16/16 ✅
- MainWindowSmokeTest: 10/10 ✅
- 全量 ctest: 58/65 通过（7 个预存失败，零回归）

---

## V15.5: 文本工具 + 文件工具注册为内置 Skill

### 背景
CodeXX UI 中的「工具」按钮（JSON格式化/压缩/Markdown清理/文本清理）和「文件工具」按钮（读文件/列目录/保存/打开路径）已在 AgentToolRegistry 中注册为 Function Calling tools，但未注册为 SkillManager 的 Skill，导致 AI 在 prompt 中无法按 skill 名称匹配。

### 修改内容
1. **`src/skills/SkillManager.cpp`**:
   - `scanDirectory`: `entryInfoList(QDir::Files)` → `QDirIterator::Subdirectories`
   - 支持递归扫描子目录下的 SKILL.md（对齐 WorkBuddy skill 目录规范）
   - 附带修复了预存测试 SkillManagerTest::TC-10 `totalCount()==2`

2. **`.workbuddy/skills/`** 创建 8 个 SKILL.md:

| 目录 | 技能名 | 触发词示例 |
|------|--------|-----------|
| text-json-format | text.json_format | JSON格式化, format json, 美化JSON |
| text-json-compact | text.json_compact | JSON压缩, compact json, JSON单行 |
| text-markdown-cleanup | text.markdown_cleanup | Markdown清理, md整理, 格式化markdown |
| text-cleanup | text.cleanup | 文本清理, 清理文本, 统一换行 |
| file-read-text | file.read_text | 读取文件, read file, 查看文件 |
| file-list-directory | file.list_directory | 列出文件夹, list directory, 目录内容 |
| file-save-text | file.save_text | 保存文件, save file, 写入文件 |
| file-open-path | file.open_path | 打开文件, open folder, 用资源管理器打开 |

### 测试结果
- SkillManagerTest: 11/11 ✅ (之前预存失败 TC-10 修复)

---

## V17.1 BugFix: DeepSeek API 图片格式修复 + 预览栏位置调整 (10:27)

### 问题1: 图片发送失败 `unknown variant image_url, expected text`

**根因**: DeepSeek API **不支持 OpenAI 的 `image_url` 格式**。DeepSeek 要求：

```json
// DeepSeek 格式（正确）:
{"type": "image", "image": {"data": "<raw_base64>", "format": "base64"}}

// OpenAI 格式（DeepSeek 不支持）:
{"type": "image_url", "image_url": {"url": "data:image/png;base64,..."}}
```

**两个改动**:
1. `"image_url"` → `"image"` 
2. `{"image_url": {"url": "..."}}` → `{"image": {"data": "...", "format": "base64"}}`
3. 剥离 `data:image/png;base64,` 前缀，只传原始 base64

**影响文件**: `src/services/OpenAICompatibleClient.cpp`（~10行改动）

### 问题2: 图片预览栏位置

**改动**: 图片预览栏从输入框**上方**（独立区域）移到输入框**内部下方**（与文字一起，属于同一个 composer 容器）

- `composerRow`（QTextEdit + 按钮）→ `m_imagePreviewBar`（紧凑图片卡片）
- 左右 margin 对齐（28px），视觉上属于输入区域的一部分

**影响文件**: `src/ui/MainWindow.cpp`（setupUi 布局调整）

### 测试
65/65 全部通过 ✅，零回归

---

## V17.2: 图片内联嵌入 QTextEdit（10:45）

**需求**: 图片直接嵌入文字输入框，与文字共存在同一个 QTextDocument 中（类似 ChatGPT/微信的图片附件模式）

**实现**: `QTextCursor::insertImage()` 在光标位置插入缩略图（max 200px），作为 QTextDocument 内联资源

**改动**:
| 模块 | 改动 |
|---|---|
| `MainWindow.h` | 删除 `m_imagePreviewBar`、`m_hoverPreviewPopup`、`rebuildImagePreviewBar()`、`removePendingImage()`；移除 `#include <QPointer>` |
| `MainWindow.cpp` | `onImagePasted()`: 插入内联缩略图 + 存 base64；`clearPendingImages()`: 只清 vector；`eventFilter()`: 移除 hover popup 逻辑 |
| setupUI | QTextEdit: `setFixedHeight(88)` → `setMinimumHeight(88)` + `setMaximumHeight(200)`；删除独立 `m_imagePreviewBar` 创建和布局 |

**体验变化**:
- Ctrl+V 粘贴图片 → 缩略图出现在光标位置，文字在图片前后自然流动
- QTextEdit 高度可自适应（最小 88px，最大 200px）
- 删除图片：直接选中后按 Backspace/Delete（图片是 QTextDocument 的一部分）
- 发送时文本取自 `toPlainText()`，图片取自 `m_pendingImages`

**测试**: 65/65 ✅，零回归

---

## V17.3: 图片发送自动降级 — DeepSeek API 不支持图片 (11:28)

### 调研结论
经查 DeepSeek 官方 API 文档 (`api-docs.deepseek.com/quick_start/pricing`)：
- `deepseek-v4-pro` 和 `deepseek-v4-flash` 均为**纯文本模型**，不支持图片输入
- "识图模式"于 2026/04/29 在网页端灰度上线，**API 端口尚未开放**（预计下半年）
- 阿里云百炼文档也确认："DeepSeek 模型仅支持文本输入，不支持图片或文档输入"

### 修复
`sendCurrentMessage()` 改为自动降级：
- 有图片时 → 剥离图片，只发送文字
- 状态栏提示："已剥离 N 张图片 — 当前模型不支持图片输入"
- 只有图片没有文字 → 提示用户不发送
- 保留 `sendMessageWithImages()` 代码路径，等 DeepSeek API 支持后启用

### 保留
- `sendMessageWithImages` / `buildRequestBody` 中的 DeepSeek 格式代码全部保留
- 内联图片展示不受影响（Ctrl+V 粘贴 → QTextEdit 内显示缩略图）
- 后续切换模型（如 GPT-4V、Claude）时可直接启用图片发送

---

## BugFix: Agent 计划成功仍跑满 15 轮 OODA 循环 (12:12)

### 问题
用户报告："agent执行了一个指令成功生成了，但是她仍然跑了15步" 以及 "她说自己没成功"

**根因**: `ApplicationController.cpp` 中 3 处 `executePlanAndReportToChat()` 调用之后，**无条件**检查 `isAgentLoopActive()` 并进入 OODA 循环：
- 计划有 1 步 → 执行成功 → 报告执行结果 → 但 OODA 循环又跑了 15 轮
- 中间某轮 LLM 可能说"我做不到"（权限/路径问题），产生不一致认知

### 修复

```
旧：executePlanAndReportToChat(plan);  // 返回 void
    if (isAgentLoopActive())           // 无条件继续循环
        continueAgentLoop();           // 最多 15 轮

新：bool allSuccess = executePlanAndReportToChat(plan);  // 返回 bool
    if (allSuccess)                   // 全部成功 → 直接结束
        cancelAgentLoop() + "任务完成";
    else                              // 有失败 → 才进 OODA 尝试恢复
        continueAgentLoop();
```

### 改动文件
| 文件 | 改动 |
|---|---|
| `AgentOrchestrator.h` | `void executePlanAndReportToChat` → `bool executePlanAndReportToChat` |
| `AgentOrchestrator.cpp` | 返回值: `failCount == 0` |
| `ApplicationController.cpp` | 3 处调用改为条件分支 |

### 测试
65/65 ✅ + AgentLoopControllerTest ✅ + AgentLoopExecutionTest ✅

---

## V17.4: 文件操作权限开放 — 移除工作目录沙箱 (12:23)

### 需求
1. Agent 文件操作不再局限于工作目录，可访问任意路径
2. 工作目录仅作为"未指定路径时的默认保存/读取位置"

### 改动
**核心**: `WorkspacePolicy::evaluateWorkspaceOperation()` — 移除 `isPathInsideWorkspace()` 门禁检查

保留的安全措施:
- ✅ `isUnsafeWorkspaceRoot()` — 拒绝 C:\、C:\Windows、Program Files 作为工作目录
- ✅ `isProtectedPath()` — 拒绝写入/删除 .git, .env, *.key, *.pem 等敏感文件

**Prompt 更新**: `AgentPlanPromptBuilder.cpp` — 提示词从 "仅限工作目录内" 改为 "工作目录为默认路径，可访问任意位置"

**测试更新**: 3 个测试文件修改
| 文件 | 改动 |
|---|---|
| `WorkspacePolicyTest.cpp` | 2 处 `!allowed` → `allowed`（../ 和绝对路径） |
| `WorkspaceFileServiceTest.cpp` | 2 处 `!ok` → `ok`（路径穿越写文件） |
| `AgentLoopControllerTest.cpp` | 失败场景从"外部路径"改为"受保护文件 .env" |
| `AgentPlanExecutorTest.cpp` | 1 处 `!ok` → `ok` |

**测试**: 65/65 ✅，零回归

---

## V17.5: Agent 中断恢复重构 — 弹窗→自然语言"继续" (12:43)

### 需求
1. 去掉启动时"是否继续执行"弹窗
2. 改为自然语言触发：用户在 Agent 模式输入"继续"/"continue" 自动恢复
3. 中断时自动保存状态，新任务自动覆盖旧状态

### 改动

**删除**:
| 文件 | 删除内容 |
|---|---|
| `AgentOrchestrator.h` | `agentResumeAvailable` 信号 |
| `ApplicationController.h` | `agentResumeAvailable` 信号 |
| `ApplicationController.cpp` | 信号转发 connect |
| `MainWindow.cpp` | QMessageBox 恢复弹窗 (~15行) |

**新增**:
- `sendAgentLoopMessage()`: 检测"继续"/"continue"/"resume"关键词
  - 有可恢复状态 → `resumeAgentLoop()` + 构建下一步 prompt → 发送 AI 请求
  - 无可恢复状态 → 提示"没有可恢复的任务"
- `cancelAgentLoop()`: 不再清除状态文件，保留以供恢复

**流程变化**:

```
旧: 启动→检测残留→弹窗"是否继续?"→Yes/No
新: 启动→静默加载状态(无弹窗)
   用户输入"继续"→检测m_agentLoopState.isValid()→自动恢复
   新任务→startAgentLoop()→自动覆盖旧状态
```

**测试**: 65/65 ✅，零回归

---

## BugFix: LLM 仍认为工具"仅限工作目录" (12:57)

V17.4 只改了代码逻辑，但 LLM 看的工具描述和提示词里仍写着"仅在工作目录内"。

| 文件 | 修复 |
|---|---|
| `AgentToolRegistry.cpp` (5 个 workspace 工具) | 描述改为"相对路径基于工作目录，绝对路径直接操作" |
| `AgentPlanPromptBuilder.cpp` | "may only operate inside workspace" → "work anywhere on filesystem" |
| `AgentLoopPromptBuilder.cpp` | 同上 |
| `AgentPlanPromptBuilderTest.cpp` | 更新断言匹配新文本 |

65/65 ✅

---

## BugFix: 统一模式计划不完整 (13:09)

"删除桌面 123.txt" → LLM 只生成 1 步 `system.path(kind="desktop")` → V17.4 修复后 "成功→结束" → 删除从未执行。

根因: Unified prompt 没说"要包含全部步骤"，LLM 把获取路径当作完整计划。

修复: `AgentPlanPromptBuilder.cpp` 提示词改为 "reply with a COMPLETE plan containing ALL steps needed to fully achieve the goal"

65/65 ✅
- 全量: 59/65 (+1), 零回归

---

## V18: Agent 能力全面优化 (15:50-16:10)

基于代码实勘重评估结果，实施 7 项优化，65/65 零回归。

### 改动汇总

| 优先级 | 项 | 文件 | 改动 |
|--------|---|------|------|
| P0-1 | 工具结果裁剪 | `AgentLoopController.cpp` | 新增 `trimObservationOutput()`；超 50 行保留首 20 + 尾 20 |
| P0-2 | 异步重复动作检测 | `AgentOrchestrator.h/.cpp` | `m_actionFingerprints` + 同参检测 + 警告注入 |
| P0-2 | 同步重复动作检测 | `AgentLoopController.cpp` | `seenActions` + 3 次重复 → RepeatedAction 终止 |
| P1-1 | Stop Hook（终止前自检） | `AgentLoopPromptBuilder.cpp` | 提示词注入：done=true 前必须验证目标是否完成 |
| P1-2 | Microcompact（观测压缩） | `AgentLoopPromptBuilder.cpp` | `compactObservations()`：保留最近 5 条完整，早期截断到 80 字符 |
| P1-3 | 轻量子代理 explore | `AgentOrchestrator.h/.cpp` | `agent.explore` 工具：只读工具集 + 30s 超时 + QEventLoop 同步等待 |
| P2-1 | Reactive 上下文压缩 | `ApplicationController.h/.cpp`, `AgentOrchestrator.h/.cpp` | context_length_exceeded → 压一半 observation → 最多重试 3 次 |
| P2-2 | 输出截断自动续接 | `StreamParser.h/.cpp`, `AIClient.h`, `OpenAICompatibleClient.h/.cpp`, `ApplicationController.h/.cpp` | finish_reason=length → responseTruncated 信号 → 自动续接 "从断点继续" |
| BugFix | delete_file 使用 Recycle Bin | `WorkspaceFileService.cpp` | QFile::moveToTrash() 优先 → 回退 .trash |
| Test | 更新 delete 测试断言 | `WorkspaceFileServiceTest.cpp`, `AgentPlanExecutorTest.cpp` | 断言兼容 Recycle Bin + .trash 两种路径 |

### 编译与测试
- 构建：零编译错误，零链接错误
- 测试：65/65 ✅（100%），零回归

---

## 14:31 — Agent 执行流程分析（三大 Agent 对比）

- 回答用户关于 Agent 执行命令流程的问题
- 绘制三大 Agent（Codex CLI / Claude Code / CodeXX）架构对比图
- 提出 10 项优化建议，按 P0/P1/P2 分级

## 15:43 — 基于代码实勘的优化空间重评估

逐文件审核以下源码后重评估：

| 文件 | 分析结果 |
|------|----------|
| `AgentOrchestrator.cpp` | 每次 `buildNextLoopPrompt()` 重建 toolRegistry（可缓存）；`executePlanAndReportToChat()` UI 裁剪 300 字符但 AgentStepRecord 存 500 字符 |
| `AgentLoopController.cpp` | `runPlan()` 有重复动作检测（fingerprint），但 `executeLoop()` 无；observation 全量追加无裁剪（第 626 行） |
| `AgentLoopPromptBuilder.cpp` | 所有内容一次性拼接，无静态/动态分离；含感知+操作引导 |
| `AgentLoopActionParser.cpp` | JSON 解析前 extractJsonObjectCandidate + stripMarkdownFence；解析失败不终止 |
| `ApplicationController.cpp` | `handleRequestFinished()` 中 UnifiedAgent 路径：有 tool_calls → 执行 → 循环；无 tool_calls 且 Chat → 终止（已隐含双重判断）；`continueAgentLoopWithToolResults()` 追加 observation 到循环 |
| `ContextWindowManager.cpp` | 超 85% 阈值时保留最近 3 轮 + SummaryAPIClient 生成摘要归档；简单截断模式，无分层 |
| `ProjectMemoryManager.cpp` | `buildMemorySection()` 注入 L1/L2/L3 + 压缩摘要；V13.2 周压缩需手动触发；`buildCompressionPrompts()` + `applyCompression()` 两步 |

### 发现

1. **重复动作检测**: `runPlan()` 同步路径有，异步 UI Agent 路径完全缺失
2. **工具结果裁剪**: UI 显示层有裁剪（300/500 字符），但 `executeLoop()` 的 observation 注入无裁剪
3. **finish_reason**: 全 src 零匹配，完全不检查 API 返回的截断信号
4. **子代理**: 全 src `sub.?agent|spawn|fork.*agent` 零匹配
5. **提示词缓存**: `buildNextLoopPrompt()` 每次重建完整 toolRegistry，无静态/动态分离
6. **上下文压缩**: 有请求级截断和记忆级周压缩两套独立系统，但缺少中间层（Microcompact）

### 重评估结果

| 优先级 | 项 | 变更 |
|--------|----|------|
| P0 | 工具结果裁剪（observation 注入时） | 未变 |
| P0 | 重复动作检测（异步路径） | 未变 |
| P1 | Stop Hook（终止前自检） | 从 P0 降级 |
| P1 | Microcompact（工具结果压缩） | 从"完整 4 层压缩"缩减 |
| P1 | 轻量子代理（只读工具） | 从"完整子代理"缩减 |
| P2 | Reactive 压缩（API 报错时） | 新增 |
| P2 | 输出截断自动续接 | 从 P1 降级 |
| ~~P1~~ | 4 层完整压缩 | 删除（范围过大） |

## V18.1: 补齐文件操作工具三件套 (17:02-17:05)

对比 Claude Code / Codex CLI 文件操作设计后，补齐三个核心缺失工具。

### 新增工具

| 工具 ID | 名称 | 功能 | 风险 |
|---------|------|------|------|
| `file.edit_text` | 精确编辑 | old_str→new_str 字符串替换，仅匹配一次 | Medium |
| `file.grep` | 内容搜索 | 正则递归搜索，返回 file:line:content | Low |
| `file.delete_directory` | 删除目录 | QDir::removeRecursively + 系统目录保护 | High |

### 改动文件

| 文件 | 改动 |
|------|------|
| `FileInteractionService.h` | 新增 editTextFile/grep/deleteDirectory 声明 |
| `FileInteractionService.cpp` | 实现三个函数（~140 行），新增 QDirIterator/QRegularExpression 依赖 |
| `AgentToolRegistry.cpp` | registerFileTools 尾部注册 3 个新工具 |
| `.workbuddy/skills/` | 3 个新 SKILL.md |

### 设计细节
- `file.edit_text`：对齐 Claude Code 的 old_str→new_str 精确替换；单匹配成功、零匹配报错、多匹配提示加上下文
- `file.grep`：正则引擎 + 文件大小保护（>1MiB 跳过）+ 50 结果上限 + glob 过滤
- `file.delete_directory`：系统目录黑名单保护（C:\Windows, /etc, /usr 等）

### 测试
- 构建：零编译错误
- 测试：65/65 ✅，零回归

## V18.2: 命令/桌面/网络操作全面补齐 (17:21-17:50)

按优先级实施 P0+P1 能力升级，65/65 零回归。

### 新增工具

| 层 | 工具 ID | 名称 | 风险 | 说明 |
|----|---------|------|------|------|
| 命令 | `command.bash` | 通用命令执行 | High | 替代 6 个硬编码命令；危险命令黑名单；30s 超时；4KiB 输出裁剪 |
| 桌面 | `system.clipboard_read` | 读剪贴板 | Low | QApplication::clipboard() |
| 桌面 | `system.clipboard_write` | 写剪贴板 | Medium | QApplication::clipboard() |
| 桌面 | `input.mouse_click` | 鼠标点击 | High | SetCursorPos + SendInput(MOUSEEVENTF) left/right/middle |
| 桌面 | `input.mouse_scroll` | 滚轮 | Medium | MOUSEEVENTF_WHEEL，delta 控制方向 |
| 网络 | `web.http_get` | HTTP GET | Medium | QNetworkAccessManager + QEventLoop 同步；15s 超时；64KiB 截断 |
| 网络 | `web.http_post` | HTTP POST | High | JSON body + application/json 头 |

### 改动文件

| 文件 | 改动 |
|------|------|
| `AgentToolRegistry.cpp` | +180 行：command.bash、剪贴板×2、鼠标×2、HTTP×2；移除 registerPerceptionTools 后的 clip；移除 registerInputTools 后的 click/scroll；新增 registerWebTools；新增 QApplication/QClipboard/QNetworkAccessManager/QEventLoop/QUrl/QTimer 头文件 |
| `InputSimulator.h/.cpp` | mouseClick/mouseScroll/moveMouse，Win32 SendInput 实现 |

### 测试
- 构建：零编译错误
- 测试：65/65 ✅

## V18.3: 桌面交互全面强化 (18:15-18:30)

新增 13 个工具，65/65 零回归。覆盖：file.copy/move/append_text/get_info、system.active_control/screen_size/window_rect/selected_text/wait_for_window、input.mouse_drag/position/key_press、web.download_file、system.open_url。

改动文件：FileInteractionService (+4 func), InputSimulator (+3 func), AgentToolRegistry (+13 tools, +windows.h/UIAutomation.h)

## V18.4: Agent 步骤显示优化 (18:37)

问题：Agent 执行大任务后会出现一大串工具调用步骤卡片（如 Step 5-18 全是工具调用），无法折叠，占用大量空间。

修复：新增 `AgentStepGroupWidget` — 将所有步骤分组为一个可折叠的摘要卡片。

### 效果
- 执行中：实时显示 `▶ [3/3] file.read_text, workspace.write_text, ...`
- 完成后：自动折叠为 `✅ Completed: N steps — tool1, tool2, ...`
- 点击展开：显示所有子步骤详情
- 默认折叠：不再占用大量聊天空间

### 改动
| 文件 | 改动 |
|------|------|
| `AgentStepGroupWidget.h/.cpp` | 新增分组卡片组件 |
| `CMakeLists.txt` (ui) | 添加新文件 |
| `MainWindow.h/.cpp` | 用 group 替换个体步骤；连接 agentLoopCompleted 触发 finish |

## V18.5: 价值工具补齐 (19:50-19:58)

新增 5 个工具/能力，65/65 零回归。

| 工具 | 功能 | 实现 |
|------|------|------|
| `file.archive` | 压缩为 zip | PowerShell Compress-Archive |
| `file.extract` | 解压 zip | PowerShell Expand-Archive |
| `code.run` | 运行 Python/JS | QProcess 临时文件 30s 超时 8KiB 裁剪 |
| `file.watch` | 文件监听 | QFileSystemWatcher start/stop/status |
| AutoFix | 编辑后自动构建+测试 | AgentOrchestrator 检测编辑 → cmake+c测试 → 注入 observation |

改动：AgentToolRegistry (+140行), AgentOrchestrator (+40行 autoFix), QFileSystemWatcher 头文件

## V18.6: 智能工具选择 (20:07-20:17)

三合一实现，65/65 零回归。

### 1. 意图感知工具排序
- `classifyGoal()`: 关键词匹配 → 7 种意图（CodeEdit/Search/BuildTest/FileManage/DesktopOp/WebRequest/ShellCmd）
- `reorderToolsByIntent()`: 匹配工具排前面加 ⭐，非匹配软限制 30 个

### 2. 工具最佳实践提示
- `buildToolGuidance()`: 从静态映射 + MEMORY 注入使用技巧（最多6条）
- 格式：`[Tool Usage Tips] • file.edit_text: 精确替换... • command.bash: 执行构建/测试...`

### 3. 工具序列记忆
- `recordToolSequence()`: 完成后写入 `.workbuddy/memory/tool-usage.md`
- 格式：日期 | tool1 → tool2 → tool3 | Goal: 摘要

### 改动
| 文件 | 改动 |
|------|------|
| `AgentLoopPromptBuilder.h/.cpp` | 新增 classifyGoal, reorderToolsByIntent, buildToolGuidance, recordToolSequence (~200行) |
| `AgentOrchestrator.cpp` | 循环完成后调用 recordToolSequence |
