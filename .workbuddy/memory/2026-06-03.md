# 2026-06-03

## V12.2 QA & 改进项 (C)

### QA 回归
- 49 测试运行，43/43 可执行通过（6 个 exe 未编译，预存问题）
- Part B 代码审查：11/13 PASS，2 项设计级差异（非运行时缺陷）
- Part C 安全审查：3/3 PASS

### C: QA 改进项修复
- C1: 移除 AgentPlanDialog.cpp 中 `catalog` 过滤死代码（344-350行）
- C2: AgentLoopController.h 中 `maxRuntimeMs` 默认值 60000→120000（AgentLoopOptions + LoopTerminationPolicy）
- C3: AgentPlanDialog.cpp 中 `RepeatedAction` 从 default → 显式 case，添加中英文状态消息
- 全量测试验证：43 可执行全通过

### V12.3 流式工具执行 — 完成

**目标**: 让工具在模型 streaming 期间就开始执行，不等 [DONE]

**架构**: StreamParser 在 tool call arguments 完整 JSON parse 成功时即发出 blockEvent → OpenAICompatibleClient 新信号 toolUseBlockComplete → ApplicationController 即时执行 + 缓存结果

**修改文件 (7)**:
- StreamParser.h/cpp: 新增 ContentBlockEvent 结构体、m_emittedToolCalls 去重机制、appendToolCallDeltas 中 JSON 完成检测
- OpenAICompatibleClient.h/cpp: 新增 toolUseBlockComplete(toolName, arguments) 信号
- ApplicationController.h/cpp: PendingToolResult 缓存结构体、handleToolUseBlockComplete 槽、请求结束时组合流式结果

**新增测试**: StreamParserV12Test.cpp (8 测试)：no-tool-call、single-tool、multi-tool、partial-text、duplicate-prevention、arguments-not-json、reset-clears-state、stream-then-text

**测试结果**: 50/50 通过（含 6 个预存 MinGW linker 问题）

### V12.4 Chat 模式自动执行工具 — 完成

**目标**: Chat 模式下新增「自动执行」开关，默认不执行工具；开启后 Chat 消息携带 tools schema，AI 自主判定调用工具并 streaming 期间即时执行

**架构**: 新增 sendMessageWithTools() 入口 → sendChatWithTools → 复用 V12.3 流式管线 (toolUseBlockComplete → handleToolUseBlockComplete) → 权限分级跳过 → handleRequestFinished 追加工具结果

**修改文件 (6)**:
- ApplicationController.h/cpp: sendMessageWithTools()、setChatAutoExecute()、setHighPermissionMode()、permission check 逻辑、Chat 模式路由放行、handleRequestFinished 结果追加
- MainWindow.h/cpp: "⚡自动执行" 按钮 + "高权限" 复选框、sendCurrentMessage 路由分支、setGenerating 禁用逻辑

**权限分级**:
- 普通工具 (requiresUserConfirmation=false): 自动执行
- 高权限工具 (requiresUserConfirmation=true): Chat 无高权限时跳过并提示，勾选高权限后执行

**新增测试**: ChatToolExecutionTest.cpp (8 测试): chat-no-tools-default, chat-with-tools, tools-blocked-plain-chat, tools-allowed-chat-auto, high-perm-skipped, high-perm-executed, pending-results-appended, cancel-resets-auto-execute

**测试结果**: 53/53 可执行全部通过（8 新增 + 45 回归 + 6 预存 MinGW linker 问题）

### V12.5 Agent 自动执行 + 移除计划弹窗 — 完成

**目标**: 移除 AgentPlanDialog 弹窗，Agent 模式下 AI 返回的计划自动执行，结果整合到聊天消息。V12.3 流式结果和 V12.4 权限分级逻辑不变。

**架构变化**:
- 旧: handleRequestFinished → emit agentPlanReady → MainWindow::openAgentPlanDialog → 用户手动点执行
- 新: handleRequestFinished → executePlanAndReportToChat(plan) → 自动执行全部步骤 → 结果追加到 m_currentAssistantContent → emit assistantMessageUpdated

**修改文件 (5)**:
- ApplicationController.h: 删除 agentPlanReady 信号，新增 executePlanAndReportToChat
- ApplicationController.cpp: 3 处 emit agentPlanReady 替换为 executePlanAndReportToChat；新增 80 行自动执行方法
- MainWindow.h: 删除 openAgentPlanDialog、insertToolOutputIntoInput
- MainWindow.cpp: 删除 AgentPlanDialog include、信号连接、两个方法实现
- tests/CMakeLists.txt: 新增 AgentPlanExecutor 源文件到测试链接

**executePlanAndReportToChat 输出格式**: Markdown 列表，每步显示 ✅/❌ + 编号 + 工具名/描述 + 截断输出 + 流式结果 + 成功/失败统计

**测试**: 50/51 可执行通过，1 个预存 AppLaunchSmokeTest MinGW 链接问题

### V12.6 Agent 连续循环执行 — 完成

**目标**: Agent 模式下实现 `分析→执行→判断→继续` 连续循环，直到 AI 判断任务完成。AgentPlanDialog 从构建中移除。

**循环机制**:
- sendAgentLoopMessage → sendUnifiedMessage (首轮) → AI 响应 + streaming 执行
- handleRequestFinished 每个分支后 → 收集观测 → continueAgentLoop
- 用 AgentLoopPromptBuilder::buildNextActionPrompt 构建下一轮提示词
- AI Chat 响应 = 任务完成 → 停止循环
- **V12.6-hotfix (22:10)**: 修复两个问题 — ① 系统提示词泄露到聊天消息中（continueAgentLoop 把循环提示词作为 User 消息发送），改为 set systemPrompt on loopSession；② Agent 模式下隐藏 自动执行/高权限 按钮（Agent 模式默认全权执行，不需要这些按钮）。52/52 测试通过

**修改文件 (6)**:
- ApplicationController.h/cpp: sendAgentLoopMessage + continueAgentLoop + executeAgentLoopIteration + 循环状态变量
- MainWindow.cpp: Agent 模式走 sendAgentLoopMessage，自动执行按钮在 Agent 模式可见
- CMakeLists.txt: 移除 AgentPlanDialog.h/.cpp
- AgentLoopExecutionTest.cpp: 8 新测试

**测试**: 52/52 全部通过

**V12 结项**: V12.1~V12.6 全部完成 (52 tests)

### Agent 循环上限 → 50 (22:17)

kMaxAgentLoopIterations: 10 → 50

### V13.1 三层记忆系统 — 完成 (22:30)

**新增 src/memory/ 模块**:
- MemoryEntry.h — 记忆条目 (timestamp/category/content/source)
- DailyMemoryWriter — L3 每日日志 (YYYY-MM-DD.md)
- ProjectMemoryManager — 三层记忆管理器 (L1~/.codex + L2 .workbuddy/memory + L3 daily)

**集成**:
- executePlanAndReportToChat() 末尾 → appendDailyLog 自动追加
- continueAgentLoop() → buildMemorySection 注入 systemPrompt

**安全**: 敏感内容检测 (api_key/token/password/secret/bearer/credential/private_key)，超1000字符自动截断

**测试**: ProjectMemoryManagerTest 15 断言，53/53 全部通过

### V13.2 L3参数调整 + 记忆压缩接口 (22:55)

**参数调整**:
- 单条截断: 1000→2000 字符
- 查询天数: 7→14 天
- 最多条数: 30→50 条
- 新增总预算硬上限: 30000 字符

**压缩接口**:
- `buildCompressionPrompts()` — 按 ISO 周分组，生成 LLM 压缩提示词
- `applyCompression()` — 删除原始日志 + 写入 YYYY-Www-compressed.md
- `compressedFiles()` — buildMemorySection 自动注入压缩摘要
- DailyMemoryWriter 新增 logFiles/entriesForDate/writeFile/removeFile

**测试**: 19 断言 (新增截断/分组/落盘/注入 4 个)，53/53 全部通过
